/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as main } from './app.json';

AppRegistry.registerComponent('main', () => App);
// AppRegistry.runApplication('todo', { rootTag: document.getElementById('root') });
